{
  "name": "vitalcore-pdf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sidecar PDF renderer for VitalCore reports (lab + radiology). Uses headless Chromium to render HTML with displayHeaderFooter: false, so the printed report has no browser-added date/title/URL/page-number.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "puppeteer-core": "^23.10.4"
  }
}
